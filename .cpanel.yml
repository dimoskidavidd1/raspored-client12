---
deployment:
  tasks:
    - export DEPLOYPATH=/public_html/timeflow
    - /bin/rm -rf $DEPLOYPATH/static $DEPLOYPATH/index.html $DEPLOYPATH/manifest.json $DEPLOYPATH/asset-manifest.json
    - /bin/cp -rf $CURRENT_SOURCE_DIR/build/. $DEPLOYPATH
